* Editing employee doesn't automatically fill-in their specialty --billy
* Employee and regulator creation is broken since major controller changes --billy
* Need to validate input in check score, bound by maxscore of according checktype --billy
* @ dio: Fix images to persist (we now get error message) at employee edit --billy
* @ dio: Red boxes around empty fields no longer showing. Intended or needing fix? --billy
* Confirm (or undo) delete --dionyziz
* AJAX delete everywhere --delete
